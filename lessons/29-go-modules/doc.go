// Package gomodules is a reference-only lesson — see README.md for the
// walkthrough. There is no runnable code in this package; the whole
// point is the commands you type in a fresh directory outside this
// repo (`go mod init`, `go get`, `go mod tidy`). This file exists so
// `go list ./...` sees the package.
package gomodules
